import { List, styled } from "@mui/material";

const TodosList = styled(List)({
    width: "100%",
    maxWidth: "600px",
    margin: "0 auto",
    padding: "0",
    backgroundColor: "#fff",
});

export { TodosList };